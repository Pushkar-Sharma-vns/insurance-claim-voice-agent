{
  "project": "Observe Insurance – Voice AI Claims Support Agent",
  "phase": "Phase 2 – Custom LLM (bring-your-own-brain via VAPI /chat/completions SSE)",
  "last_updated_turn": 24,
  "premise": "Phase 1 (VAPI-native) is tested + working and stays as the reliable demo fallback. Phase 2 swaps ONLY the brain: our server implements an OpenAI-compatible /chat/completions SSE endpoint that VAPI calls each turn. VAPI still owns telephony/STT/TTS/endpointing/barge-in. We own the agentic layer: orchestration, state, tool-calling, extraction, post-processing, CRM, memory.",
  "status": "PLANNING COMPLETE — all 4 sections (S1-S4) deep-planned & ready to build V1. No Phase 2 code yet. Next: build in order S1 loop -> S2 store -> S3 trace -> S4 postprocess/metrics.",
  "versioning_approach": {
    "principle": "Ship a working end-to-end slice FIRST, then layer depth. Applies to ALL 4 sections.",
    "V1": "Minimal necessary: happy path + main test case (greet -> auth -> confirm identity -> claim status -> end -> write interaction) + 1-2 edge cases, testable end-to-end.",
    "V2": "All edge cases, richer state/context, cross-call memory, full fallback map, FAQ-as-tool, full metrics, complete test cases.",
    "rule": "Every V2 idea gets parked in this schema (per-field / per-feature version tag) so nothing is lost; we decide V2 details later but discuss now."
  },
  "cross_cutting_decisions": [
    {"id": "P2-D1", "decision": "LLM brain = Gemini 2.5 Flash", "why": "Latency for voice, cheap, google-generativeai installed", "todo": "STRICT: web-verify exact model ID + SSE/tool-calling API at build time, do not assume"},
    {"id": "P2-D2", "decision": "Memory scope = cross-call memory", "why": "Recognize returning callers + recall prior interactions; strongest demo. Source = Airtable Interactions history keyed by caller"},
    {"id": "P2-D3", "decision": "Post-processing = our own LLM at call end (summary+sentiment+extraction), replaces VAPI analysisPlan", "why": "Showcases the agentic layer; full control"},
    {"id": "P2-D4", "decision": "S4 metrics = writeup + implemented compute (from logs/transcripts)", "why": "Panel narrative AND working metrics"},
    {"id": "P2-D5", "decision": "ONE SQLite DB (stdlib sqlite3, data/conversations.db) is the shared substrate for S2 (calls, messages), S3 (turns trace) and S4 (metrics-as-SQL). No new dependency.", "why": "Queryable, cross-call memory + trace + metrics from one place; ponytail single-file/per-op-connection/WAL, single-worker ceiling"},
    {"id": "P2-D6", "decision": "Cross-call memory READ-side is V1 (recognition tier pre-verification, recall tier gated behind identity_verified). claim_status never enters memory.", "why": "Delivers P2-D2 headline demo in V1 while preserving the identity invariant"},
    {"id": "P2-D7", "decision": "Metrics compute is V1 (app/metrics.py, pure SQL over the shared SQLite DB). Containment uses code-derived escalated, never the LLM.", "why": "Substrate already exists (S2/S3); real numbers for the panel ROI story at near-zero build cost"}
  ],
  "sections": {
    "S1_agent_design": {
      "title": "Agent Design & Conversation",
      "scope": "Orchestration loop, system prompt, dialogue states, conversation-flow control, context management, the agent harness, error handling + fallback responses per scenario (no-record, denied identity, alt-verification, escalation, 911 safety, off-topic).",
      "maps_to_task": ["Capabilities 1-4", "Persona & tone", "Voice flow: greeting->auth->API->response->fallback"],
      "status": "PLANNING COMPLETE — all 7 subsections decided; ready to build V1",
      "S1_SUMMARY": {
        "architecture": "Our FastAPI server exposes POST /chat/completions (OpenAI-compatible) that VAPI calls each turn; inside we call Gemini 2.5 Flash via LiteLLM (no LangGraph/LangChain). We OWN the system prompt (strip VAPI's, inject ours). VAPI keeps telephony/STT/TTS/barge-in and only consumes our streamed SSE. Reuse Phase 1 /webhooks/vapi/end-of-call for call-end persist + post-processing.",
        "two_agents": "Responder (streams reply) + parallel extractor (asyncio task, updates current_intent for next turn, lag-by-one accepted).",
        "state": "Pydantic ConversationState in an in-memory dict keyed by call.id (single-worker ceiling; Redis/SQLite=V2). Code-owned V1 fields: call_id, phase(derived), caller_phone, identity_verified, customer, turn_count, error_counts. Extractor-owned V1: current_intent (minimal). Rest of soft state = V2.",
        "context_engineering": "3 layers merged into ONE system message per turn: [static system prompt]+[dynamic state block], then VAPI-resent user/assistant turns. RENDERER-AS-GATE: claim_status withheld from context until identity_verified=true.",
        "flow_gates": "Two orthogonal axes: phase (code, auth progress) vs current_intent (extractor, soft) -> non-linear handling. Gated: claim data. Open any phase: FAQ/safety. Identity flip via confirm_identity(confirmed) tool (deterministic). V1 deny/no-record=single re-ask then human-followup; alt-verification=V2.",
        "tools_v1": "lookup_claim (reuse crm, returns NAME ONLY), confirm_identity(bool), faq_lookup(query_type=intent enum from faqs.json). Escalation/safety served via faq_lookup KB answers + 1-line prompt safety directive. No escalation tool in V1.",
        "fallbacks": "fallbacks.json, key->{first,repeat} tiers. state.error_counts tracks repeats; 2nd hit of same error escalates to human/callback with warmer/humbler tone. 7 V1 scenarios: crm_lookup_error, api_failure, timeout, llm_error, no_record, identity_denied, malformed_request.",
        "harness": "Local multi-turn sim of VAPI /chat/completions. Deterministic no-LLM gate checks + one real-Gemini E2E happy path. V1 acceptance test.",
        "v1_build_order": [
          "1. faqs loader + fallbacks.json + ConversationState (Pydantic) + in-memory store",
          "2. system prompt (static) + dynamic-block renderer (renderer-as-gate)",
          "3. tool registry: lookup_claim(reuse crm), confirm_identity, faq_lookup",
          "4. POST /chat/completions: build messages -> LiteLLM Gemini tool loop (resolve-then-stream) -> SSE; parallel extractor for current_intent",
          "5. extend end-of-call webhook: persist transcript (S2) + our-own summary/sentiment (S4)",
          "6. logging/tracing across the loop (S3)",
          "7. harness: deterministic gate checks + real-Gemini E2E happy path"
        ],
        "build_time_verifications_STRICT": [
          "LiteLLM Gemini 2.5 Flash exact model id + streaming + tool-calling API (web-verify, don't assume)",
          "VAPI incoming messages exact shape: filter role:system defensively, don't assume messages[0]",
          "VAPI end-of-call-report transcript field path (reuse Phase 1 knowledge)"
        ]
      },
      "decisions": [
        {"id": "S1-D1", "decision": "Framework = LiteLLM only (NO LangGraph/LangChain). Our server is OpenAI-compatible outward (to VAPI); LiteLLM client inward (to Gemini)", "todo": "STRICT: web-verify LiteLLM Gemini streaming + tool-calling API at build"},
        {"id": "S1-D2", "decision": "Two agents: main responder (streams the reply) + parallel extraction agent (maintains live ConversationState)", "why": "Extraction off the token-streaming path; state stays fresh without blocking latency"},
        {"id": "S1-D3", "decision": "State = Pydantic ConversationState, updated live by extractor; persisted to DB at call end (handled in S2/S4)"},
        {"id": "S1-D4", "decision": "Prompt architecture = 3 layers: static system prompt + dynamic state/memory-derived context block + turn history"},
        {"id": "S1-D5", "decision": "Non-linear flow: phases greeting/auth -> handling{status|faq|support|escalation} -> end; caller can jump intents. State+gates keep correctness, NOT a rigid FSM"},
        {"id": "S1-D6", "decision": "Fallback + error handling externalized to a JSON doc (scenario -> behavior/response)"},
        {"id": "S1-D7", "decision": "FAQ knowledge base as JSON, served via tool calling"},
        {"id": "S1-D8", "decision": "Extractor IS in V1 but minimal: extracts current_intent only. Richer soft state (topics/pending/unresolved/escalation_reason) = V2", "why": "Keeps V1 thin while still exercising the two-agent architecture"},
        {"id": "S1-D9", "decision": "FAQ V1 = tool calling with constrained query_type key (from user-provided faq.json); available keys listed in system prompt; LLM decides whether to call + which key. V2 = RAG/semantic retrieval", "todo": "USER to provide faq.json"},
        {"id": "S1-D10", "decision": "Our server OWNS the system prompt (Decision 1). VERIFIED feasible: VAPI custom-LLM is OpenAI-compatible; VAPI only consumes our streamed SSE, so we strip its incoming system message and inject our own static+dynamic prompt before the Gemini call. Config: dashboard Model->Custom LLM->URL={BASE}/chat/completions, auth header; VAPI assistant system-prompt field left as ignored placeholder. Build: filter incoming role:system defensively, don't assume messages[0]", "sources": ["docs.vapi.ai/customization/custom-llm/using-your-server", "docs.vapi.ai/customization/tool-calling-integration", "Phase1 verified_facts"]},
        {"id": "S1-D11", "decision": "S1.2 context = 3 layers merged into ONE system message per turn: [static system prompt] + [dynamic state block], followed by the VAPI-resent user/assistant turns", "why": "Single system message avoids provider ambiguity on multiple system messages"},
        {"id": "S1-D12", "decision": "S1.2 RENDERER-AS-GATE: state stores full customer incl. claim_status, but the dynamic-block renderer only exposes claim_status into context once identity_verified=true. Before that, name-only. The model literally cannot leak what isn't in its context", "why": "Strongest identity gate; deterministic, prompt-independent"},
        {"id": "S1-D13", "decision": "Sync/async model: async FastAPI (async def /chat/completions), ONE concurrent asyncio task (the extractor), everything the caller waits on stays sequential (responder tool loop; S4 post-processing inline in webhook). Blocking requests/sqlite accepted in the async handler at single-caller demo scale (ponytail); to_thread is V2. Use litellm.acompletion.", "why": "Concurrency only where it pays (extractor off the streaming path); avoids premature to_thread plumbing for a single-caller demo. See S1.4.sync_async"}
      ],
      "S1.2_context_engineering": {"status": "DECIDED", "note": "3 layers merged into one system message; renderer-as-gate withholds claim_status until verified; extractor has its own minimal prompt (current_intent only)"},
      "S1.3_flow_gates": {"status": "DECIDED", "note": "Two orthogonal axes: phase (code-derived, auth progress) vs current_intent (extractor, soft). Non-linear handling falls out. Gated: claim data (needs identity_verified). Open in any phase: FAQ/general help/safety. Identity flip via confirm_identity(confirmed:bool) tool -> code flips -> renderer exposes claim_status next turn. V1 deny/no-record = single re-ask then 'human will follow up' + log; alt-verification (name+claim_id) + verification_attempts = V2"},
      "S1.4_orchestration": {
        "status": "DECIDED",
        "entry_points": {"per_turn": "POST /chat/completions (new brain)", "call_end": "POST /webhooks/vapi/end-of-call (REUSE Phase 1) -> persist transcript (S2) + run post-processing summary/sentiment (S4) from VAPI-provided transcript"},
        "state_store_v1": "In-memory dict keyed by call.id, each value a Pydantic ConversationState. Single-worker ceiling (ponytail). Chosen over stateless-reconstruct because cross-turn tool-result resending by VAPI is undocumented/quirky.",
        "state_store_v2": "Redis/SQLite for multi-worker; revisit stateless-reconstruct.",
        "per_turn_flow": "extract call.id+customer.number -> get/create state -> build messages (strip VAPI system, prepend our static+dynamic block, keep turns) -> responder tool loop (lookup_claim/confirm_identity/faq_lookup) -> stream final SSE -> fire extractor in parallel (asyncio task) updating current_intent for next turn (lag-by-one accepted)",
        "tool_streaming_v1": "resolve-then-stream: resolve tool calls with non-streamed model calls, then stream the final answer. Simple; small latency only when a tool fires.",
        "sync_async": {
          "model": "Async event loop, ONE concurrent background task, everything the caller directly waits on stays sequential.",
          "async": ["/chat/completions handler = async def (must be, to fire the extractor task + stream SSE)", "extractor = asyncio task, fire-and-forget, updates current_intent for the NEXT turn (lag-by-one)", "turn-trace SQLite write deferred until after SSE stream closes (off latency path)"],
          "sync": ["responder tool loop within a turn = sequential (resolve-then-stream; caller is waiting, nothing to parallelize; parallelizing = V2_TOP_PRIORITY)", "S4 post-processing = inline/synchronous in end-of-call webhook (this is what lets us drop Phase 1 GET /call/{id} polling)"],
          "blocking_call_gotcha": "crm.py uses blocking `requests`; sqlite3 is blocking. In an async handler these block the event loop. V1 DECISION: `# ponytail: blocking requests/sqlite in async handler; fine at single-caller demo scale`. Wrap in asyncio.to_thread ONLY if/when concurrency matters (V2). LiteLLM: use acompletion in the async handler."
        },
        "V2_TOP_PRIORITY": "Tool-calling + streaming optimization (stream-with-tool-interception, lower latency). User flagged this as the MOST IMPORTANT V2 work.",
        "evidence": ["VAPI resends conversation history each turn (confirmed)", "within-turn tool execution is ours", "cross-turn tool-result persistence undocumented -> use own store"]
      },
      "S1.5_tools_faq": {
        "status": "DECIDED",
        "v1_tools": {
          "lookup_claim": "param phone_number (default call.customer.number); crm.lookup_by_phone -> state.customer; RETURNS NAME ONLY (never claim_status)",
          "confirm_identity": "param confirmed:bool; true -> flip identity_verified + phase HANDLING; false -> V1 human-followup path",
          "faq_lookup": "param query_type = intent enum from faqs.json; returns knowledge_base.faqs[intent].answer"
        },
        "faqs_json": "Structure: knowledge_base{company_name, support_email, faqs:[{category, intent, questions[], answer}]}. query_type enum = the 8 intents: office_hours, mailing_address, start_new_claim, general_claims_process, submit_documentation, human_representative, emergency_911, out_of_scope. Loader maps intent->answer. questions[] = example phrasings for the system prompt (V2: embeddings/RAG).",
        "escalation_safety": "Served via faq_lookup (KB owns human_representative/emergency_911/out_of_scope answers). PLUS a one-line safety directive kept in system prompt as belt-and-suspenders so 911 wording is right even if the tool isn't called.",
        "note": "No separate escalation tool in V1; V2 adds schedule_callback + escalation_reason."
      },
      "S1.6_fallback_error": {
        "status": "DECIDED",
        "approach": "Externalize to fallbacks.json (key -> {first, repeat} tiers). Handler tracks state.error_counts[key]; 1st hit -> first tier (retry/standard), 2nd hit of SAME error -> repeat tier (escalate to human/offer_callback, warmer+humbler+gentle-sales tone). Returned as a normal assistant SSE message, never a raw 500.",
        "tier_structure": "each key = {first:{response,action}, repeat:{response,action}}",
        "v1_scenarios": ["crm_lookup_error(offer_callback)", "api_failure(human_followup)", "timeout(offer_callback)", "llm_error(human_followup)", "no_record(human_followup)", "identity_denied(human_followup)", "malformed_request(safe_ack)"],
        "repeat_tone": "friendly, humble, apologetic, gentle-sales: e.g. 'I'm so sorry for the hold-up — rather than keep you waiting I'll have one of our representatives call you back personally. We really appreciate your patience.'",
        "taxonomy": "crm_lookup_error=Airtable-specific; api_failure=model/transport-level; timeout=time budget; llm_error=bad content",
        "v2": "alt-verification branch, retry-with-backoff, verification_attempts exhaustion, configurable threshold (>2), per-error escalation reasons"
      },
      "subsections": {
        "S1.1": "State design — Pydantic ConversationState (fields, ownership: code-set vs extractor-set)",
        "S1.2": "Context engineering — 3-layer prompts (system / dynamic state+memory / turn) + how dynamic block is assembled",
        "S1.3": "Flow & gates — phases, non-linear intent routing, identity gate before claim reveal",
        "S1.4": "Orchestration — LiteLLM loop: responder + parallel extractor, streaming, tool loop",
        "S1.5": "Tools & FAQ KB — lookup_claim (reuse crm), faq.json lookup, escalation/transfer",
        "S1.6": "Fallback & error handling — the JSON map (scenario -> response/behavior, error tiers)",
        "S1.7": "Harness/testing — local simulation of VAPI /chat/completions to test the flow"
      },
      "S1.1_state_design": {
        "status": "DECIDED",
        "ownership": "identity_verified + phase are code-owned (deterministic, zero-lag, protect the gate); soft fields are extractor-owned (lag-tolerant)",
        "schema_fields": {
          "call_id": {"owner": "code", "version": "V1"},
          "phase": {"owner": "code-derived", "version": "V1", "note": "greeting->authenticating->confirming_id->handling->escalating->ending"},
          "caller_phone": {"owner": "code", "version": "V1"},
          "identity_verified": {"owner": "code", "version": "V1", "note": "flipped on caller confirmation; gates claim reveal"},
          "customer": {"owner": "code", "version": "V1", "note": "name/claim_id/claim_status from lookup_claim result"},
          "turn_count": {"owner": "code", "version": "V1"},
          "error_counts": {"owner": "code", "version": "V1", "note": "dict[error_type -> count]; drives 2-tier fallback: 1st hit retry/standard, 2nd hit of SAME error -> escalate to human/callback with warmer message"},
          "current_intent": {"owner": "extractor", "version": "V1", "note": "MINIMAL V1 extractor field — feeds FAQ routing + which query_type to call; extractor runs but only this in V1"},
          "topics_discussed": {"owner": "extractor", "version": "V2"},
          "pending_action": {"owner": "extractor", "version": "V2"},
          "unresolved": {"owner": "extractor", "version": "V2"},
          "verification_attempts": {"owner": "code", "version": "V2", "note": "added per user; drives alt-verification fallback"},
          "escalation_reason": {"owner": "extractor/code", "version": "V2", "note": "added per user"}
        }
      },
      "v1_scope": "Responder agent + minimal parallel extractor (current_intent only) + deterministic state + identity gate. FAQ via tool calling with query_type keys (keys in system prompt). Safety/911/escalation scripted in system prompt.",
      "v2_scope": "Full soft state, non-linear intent routing, cross-call memory injection, full fallback JSON map, FAQ RAG, verification_attempts/escalation_reason logic.",
      "S1.7_harness": {
        "status": "DECIDED",
        "approach": "Local script simulates VAPI multi-turn POSTs to /chat/completions (no telephony). Two layers: (1) deterministic no-LLM checks on safety-critical logic (renderer gate withholds claim_status until verified, confirm_identity flip, error_counts 2nd-hit escalation, phase derivation); (2) one real-Gemini E2E happy path: greet->phone->lookup_claim->confirm->status->faq_lookup->end-of-call webhook asserts interaction row + summary/sentiment.",
        "role": "V1 acceptance test (CLAUDE.md goal-driven). S1 not done until this passes."
      },
      "faqs_json_received": true,
      "open_questions": [],
      "next_steps": ["User choice: plan S2-S4 first, or start building S1 V1 now"]
    },
    "S2_crm_memory": {
      "title": "CRM Integration (Airtable) & Memory",
      "scope": "Airtable read (lookup_by_phone) + write (interactions), storing the conversation, memory management (in-turn state, persistent transcript, cross-call memory).",
      "maps_to_task": ["Integration 1: retrieval by phone", "Integration 2: write-back record"],
      "status": "PLANNING COMPLETE — deep-planned; ready to build V1 (after S1 orchestration loop exists)",
      "reuse": "crm.py lookup_by_phone/write_interaction reused verbatim from Phase 1.",
      "S2_SUMMARY": {
        "store": "ONE SQLite DB (stdlib sqlite3, no new dep) at data/conversations.db serves BOTH S2 (calls+messages) and S3 (turns). Chosen over JSONL/Airtable-table for queryability, cross-call-memory reads, and S4-metrics-as-SQL.",
        "store_division": "Airtable Interactions = business/CRM record (name, summary, sentiment, timestamp), reused verbatim, panel-visible. SQLite = transcript + per-turn trace + cross-call-memory substrate. Both written at call end; turns written per-turn during the call (S3).",
        "transcript": "Belt-and-suspenders: (a) vapi_transcript string on the calls row = authoritative spoken record from VAPI end-of-call-report; (b) messages rows = our structured history (roles + tool calls) = what the model actually processed.",
        "memory_v1": "Cross-call memory READ-SIDE pulled into V1 (user decision). At call start, recent_calls_by_phone(digits) queries SQLite; a memory block is injected into the S1.2 dynamic-context renderer.",
        "memory_gate": "Memory block is TWO-TIER, mirroring the identity gate. Recognition tier (name + prior-call count + last-call date) available pre-verification. Recall tier (prior-call summaries) exposed ONLY after identity_verified=true. NEVER claim_status. Renderer-as-gate [[S1-D12]] governs both claim_status and memory recall — prevents a phone-spoofed caller from hearing prior claim details.",
        "persist_path": "Extend /webhooks/vapi/end-of-call (Phase 1 route, prefix /webhooks): read in-memory ConversationState by call.id -> run S4 summary/sentiment -> write Airtable Interaction (reused) -> write SQLite calls + messages rows."
      },
      "decisions": [
        {"id": "S2-D1", "decision": "Unified SQLite DB at data/conversations.db (stdlib sqlite3, WAL mode, short-lived per-operation connection like the per-line log handler). Serves S2 (calls, messages) AND S3 (turns) AND S4 (metrics-as-SQL).", "why": "Queryable, zero new dependency, one substrate for transcript+memory+trace+metrics", "ceiling": "ponytail: single DB file + per-op connection, single-worker. Move to pooled connection / Postgres if concurrency grows."},
        {"id": "S2-D2", "decision": "Two stores with distinct purposes, both written at call end: Airtable Interactions = CRM-visible business record (reused verbatim); SQLite = engineering transcript + trace + memory substrate.", "why": "Don't overload Airtable with engineering data; don't lose the panel-visible CRM write"},
        {"id": "S2-D3", "decision": "Persist transcript two ways: vapi_transcript (spoken record, from end-of-call-report) on calls row + structured messages rows (what the model processed, incl. tool calls).", "todo": "STRICT: web-verify VAPI end-of-call-report transcript field path at build (artifact.transcript vs message.transcript vs artifact.messages)"},
        {"id": "S2-D4", "decision": "Cross-call memory READ-side is V1 (pulled forward per user). recent_calls_by_phone(digits, limit) at call start -> memory block into S1.2 renderer.", "why": "P2-D2 headline demo (recognize returning callers) delivered in V1"},
        {"id": "S2-D5", "decision": "Memory block is two-tier and gated: recognition (name+count+date) pre-verification; recall (prior summaries) only after identity_verified=true; claim_status NEVER in memory.", "why": "Preserves the identity invariant against phone spoofing; consistent with lookup_claim name-only [[S1-D12]]"},
        {"id": "S2-D6", "decision": "Persist entry point = extend Phase 1 /webhooks/vapi/end-of-call (reads in-memory state by call.id).", "why": "Reuse; state still resident on same worker at call end (ponytail single-worker ceiling)"}
      ],
      "sqlite_schema": {
        "note": "CREATE TABLE IF NOT EXISTS on startup (init_db called from main.py). caller_phone_digits = last-10 join key, stored unmasked (local DB, it IS the retrieval key; masking is a LOG concern).",
        "calls": "call_id TEXT PK, caller_phone_digits TEXT, caller_name TEXT, claim_id TEXT, identity_verified INT, current_intent TEXT, final_phase TEXT, num_turns INT, summary TEXT (S4), sentiment TEXT (S4), vapi_transcript TEXT, started_at TEXT, ended_at TEXT, duration_seconds REAL, escalated INT",
        "messages": "id INTEGER PK, call_id TEXT FK, turn_index INT, role TEXT, content TEXT, tool_name TEXT NULL, created_at TEXT",
        "turns": "OWNED BY S3 (see S3.trace_schema) — per-turn trace rows in the same DB"
      },
      "v1_build_order": [
        "1. app/services/store.py (stdlib sqlite3): init_db() + save_call() + save_messages() + save_turn() + recent_calls_by_phone(digits, limit). WAL, per-op connection.",
        "2. Call init_db() on startup in main.py",
        "3. Cross-call memory read: recent_calls_by_phone -> two-tier memory block feeding the S1.2 dynamic renderer (recall tier gated on identity_verified)",
        "4. Extend /webhooks/vapi/end-of-call: after Airtable write, persist calls + messages rows from in-memory state"
      ],
      "build_time_verifications_STRICT": [
        "VAPI end-of-call-report transcript field path (don't assume; reuse Phase 1 payload knowledge + verify)",
        "In-memory ConversationState is still resident (same worker) when the end-of-call webhook fires; keyed by call.id"
      ],
      "v1_scope": "Airtable read + write-back reused from Phase 1, PLUS SQLite persist (calls+messages) at call end, PLUS cross-call memory read (recognition + gated recall) injected into context.",
      "v2_scope": "Richer memory management (semantic recall across many calls, summary-of-summaries), memory decay/relevance ranking, Redis/Postgres for multi-worker, memory write-side beyond last-call summary.",
      "open_questions": [],
      "next_steps": ["Build after S1 orchestration loop exists (S2 hooks into the per-turn context render + end-of-call webhook)"]
    },
    "S3_logging_monitoring": {
      "title": "Logging, Monitoring & Tracing",
      "scope": "Monitoring/logging touchpoints where errors + metrics are captured; per-call tracing extended to the custom-LLM loop (turns, tool calls, latencies, token counts, errors).",
      "maps_to_task": ["Deliverable: monitoring/logging touchpoints"],
      "status": "PLANNING COMPLETE — deep-planned; ready to build V1 (alongside the S1 loop)",
      "reuse": "logging_setup.py (PerCallFileHandler + call_id_var + masked phones) reused VERBATIM; extend touchpoints across the LLM loop. No change to logging_setup.py.",
      "S3_SUMMARY": {
        "two_channels": "Human-readable INFO lines -> reused per-call log/<callId>.log (Phase 1 infra). Machine-readable per-turn trace -> SQLite turns table (same DB as S2) = the substrate S4 metrics compute reads via SQL.",
        "trace_timing": "turn trace row written AFTER the SSE stream closes (background task / post-generator), so it is OFF the user-perceived latency path.",
        "tokens": "LiteLLM usage. resolve-then-stream [[S1-D... orchestration]]: non-streamed tool-resolution calls return usage directly; final streamed answer uses stream_options={'include_usage':True} -> usage on the final chunk; fallback litellm.token_counter(model, messages) if a provider omits it.",
        "timing": "stdlib time.perf_counter via a small helper. NO OpenTelemetry/tracing library in V1 (ponytail)."
      },
      "decisions": [
        {"id": "S3-D1", "decision": "Trace sink = SQLite `turns` table in the same data/conversations.db as S2. S4 metrics = SQL queries. Written per turn after streaming (off latency path).", "why": "Unifies stores; makes S4 containment/handle-time a query, not a log-parser", "sources": ["user decision this session"]},
        {"id": "S3-D2", "decision": "Metric list LOCKED for V1 (see metrics_locked). Per-turn + per-call.", "why": "V1 scope already fixed the set; no open metric question remains"},
        {"id": "S3-D3", "decision": "Token counts via LiteLLM usage; resolve-then-stream makes tool-resolution usage free; final stream needs stream_options include_usage; litellm.token_counter fallback.", "todo": "STRICT: web-verify Gemini 2.5 Flash + LiteLLM include_usage actually returns usage on the final stream chunk at build (known provider-specific gaps)", "sources": ["docs.litellm.ai/docs/completion/usage", "docs.litellm.ai/stream"]},
        {"id": "S3-D4", "decision": "Timing via stdlib time.perf_counter + a tiny span helper. No tracing lib in V1.", "why": "ponytail: OTel is V2 dashboard territory, overkill for a demo"},
        {"id": "S3-D5", "decision": "Reuse Phase 1 logging infra verbatim (PerCallFileHandler, call_id_var, mask_phone). Set call_id_var at /chat/completions entry so every LLM-loop log line lands in that call's file.", "why": "Zero new logging code; consistent per-call files across both phases"}
      ],
      "metrics_locked": {
        "per_turn_turns_table": ["turn_latency_ms", "llm_latency_ms", "prompt_tokens", "completion_tokens", "total_tokens", "tool_calls (JSON: [{name,status,latency_ms}])", "phase_before", "phase_after", "current_intent", "identity_verified", "error_type (NULL|S1.6 taxonomy)"],
        "per_call_derivable": ["num_turns", "total_tokens (SUM turns)", "duration_seconds", "escalated (containment substrate)", "final_phase", "sentiment (S4)"]
      },
      "trace_schema": {
        "turns": "id INTEGER PK, call_id TEXT FK, turn_index INT, turn_latency_ms REAL, llm_latency_ms REAL, prompt_tokens INT, completion_tokens INT, total_tokens INT, tool_calls TEXT (JSON), phase_before TEXT, phase_after TEXT, current_intent TEXT, identity_verified INT, error_type TEXT NULL, created_at TEXT"
      },
      "touchpoints_extended": [
        "/chat/completions entry: set call_id_var(call.id); INFO turn start (masked phone, turn_index)",
        "each LLM call: INFO latency_ms + token usage",
        "each tool call: INFO name/status/latency (mask phone args via crm.mask_phone)",
        "phase transition: INFO phase_before->phase_after",
        "errors: map to S1.6 fallback taxonomy; logger.exception; error_type recorded on the turn row",
        "extractor task: INFO current_intent extracted (non-blocking, its own line)"
      ],
      "build_time_verifications_STRICT": [
        "LiteLLM + Gemini 2.5 Flash: include_usage returns usage on final stream chunk (else use token_counter fallback)",
        "FastAPI StreamingResponse: run the turn-trace write after generator close / as a background task so it stays off the perceived-latency path"
      ],
      "v1_scope": "Per-turn latency, token counts, tool-call success/fail + latency, LLM errors (S1.6 taxonomy), phase transitions, current_intent — logged to reused per-call files AND persisted to the SQLite turns table for S4.",
      "v2_scope": "Aggregation/dashboards, latency percentiles, per-call cost tracking (litellm cost), OpenTelemetry spans, richer trace visualization, alerting.",
      "open_questions": [],
      "next_steps": ["Build alongside the S1 loop: add span timing + turn-trace write inside the /chat/completions orchestration; store.save_turn() from S2"]
    },
    "S4_metrics_postprocessing": {
      "title": "Data & Metrics Evaluation + Post-processing",
      "scope": "Post-call processing (our own summary + sentiment + extraction, replacing VAPI analysisPlan), and the metrics/ROI evaluation: which metrics to track and how to feed them back into prompt/logic tuning.",
      "maps_to_task": ["Deliverable: metrics & ROI; containment, handle time, prompt tuning"],
      "status": "PLANNING COMPLETE — deep-planned; ready to build V1 (after S1 loop + S2 store exist)",
      "S4_SUMMARY": {
        "postprocess": "Our own LiteLLM Gemini pass runs SYNCHRONOUSLY inside the extended /webhooks/vapi/end-of-call (P2-D3). Replaces VAPI analysisPlan AND removes the Phase 1 polling problem (we own the call, no GET /call/{id} wait). ONE flat structured-output call.",
        "extraction_v1": "Flat schema: {summary:str, sentiment:enum[Positive|Neutral|Negative], resolution_status:enum[resolved|escalated|unresolved], primary_topic:str}. Flat to dodge the nested-Pydantic 400 on Gemini. Stored: summary+sentiment -> Airtable write_interaction (reused) + SQLite calls row; resolution_status+primary_topic -> SQLite calls row.",
        "containment_source": "CODE-derived escalated (final_phase=='escalating' OR any error hit S1.6 repeat tier), NOT the LLM. LLM resolution_status is descriptive only — never trust the model for the headline metric.",
        "phase_branch": "Same webhook serves both phases. If in-memory state exists for call.id -> Phase 2 -> run OUR post-processing. Else -> Phase 1 -> keep existing VAPI-analysis behavior. Preserves the Phase 1 fallback untouched.",
        "metrics_v1": "app/metrics.py = pure SQL aggregates over data/conversations.db (no pandas). Prints a report + returns a dict. Pulled into V1 (user decision) since the S2/S3 substrate already holds everything.",
        "roi": "METRICS.md maps metrics -> ROI narrative + the prompt-tuning feedback loop. Framework/skeleton now; real numbers after live calls."
      },
      "decisions": [
        {"id": "S4-D1", "decision": "Post-processing is OUR synchronous LiteLLM Gemini call inside the end-of-call webhook (P2-D3). No VAPI analysisPlan, no GET /call/{id} polling.", "why": "We own the transcript + timing; removes the Phase 1 async-analysis timing problem noted in progress.md"},
        {"id": "S4-D2", "decision": "ONE FLAT structured-output pass -> {summary, sentiment, resolution_status, primary_topic}. response_format json_schema + litellm.enable_json_schema_validation; prompt-and-parse fallback on failure -> S1.6 llm_error tier (sentiment->Neutral, summary from transcript tail).", "todo": "STRICT: web-verify LiteLLM + gemini-2.5-flash json_schema returns valid output; keep schema FLAT (nested Pydantic 400 caveat)", "sources": ["docs.litellm.ai/docs/completion/json_mode", "ai.google.dev/gemini-api/docs/structured-output"]},
        {"id": "S4-D3", "decision": "Containment metric uses CODE-derived `escalated` on the calls row, not the LLM's resolution_status.", "why": "Headline ROI metric must be deterministic, not model-dependent"},
        {"id": "S4-D4", "decision": "Metrics compute is V1 (pulled forward): app/metrics.py, pure sqlite3 SQL aggregates, no pandas.", "why": "Substrate (calls+turns) already exists from S2/S3; compute is a thin SQL script (ponytail)"},
        {"id": "S4-D5", "decision": "Same end-of-call webhook branches on whether in-memory state exists for call.id: Phase 2 -> our post-processing; Phase 1 -> existing VAPI-analysis path.", "why": "Extend the reused webhook without breaking the Phase 1 fallback demo"},
        {"id": "S4-D6", "decision": "ROI writeup = METRICS.md (panel deliverable): metric->ROI mapping + prompt-tuning feedback loop. Skeleton now, numbers post-call.", "why": "P2-D4 narrative for the panel"}
      ],
      "metrics_locked": {
        "from_calls": ["containment_rate = 1 - escalated/total", "avg_handle_time_s = AVG(duration_seconds)", "avg_turns = AVG(num_turns)", "sentiment_distribution = COUNT GROUP BY sentiment", "resolution_distribution = COUNT GROUP BY resolution_status"],
        "from_turns": ["total_tokens = SUM(total_tokens)", "est_cost_usd = total_tokens * gemini-2.5-flash price (verify price at build)", "tool_success_rate = parse turns.tool_calls JSON, ok/total", "avg_turn_latency_ms = AVG(turn_latency_ms)", "avg_llm_latency_ms = AVG(llm_latency_ms)"]
      },
      "prompt_tuning_loop": {
        "note": "How metrics feed back into prompt/logic tuning (the task asks for this).",
        "levers": [
          "primary_topic x escalated -> topics that escalate most -> add/fix FAQ answer or prompt guidance",
          "avg_turn_latency by tool (turns.tool_calls) -> slow tool path -> S1.4 V2 stream-with-tool-interception",
          "negative-sentiment transcripts -> manual review -> tone/prompt fixes (S1.6 repeat-tier)",
          "tokens per turn trend -> context-size tuning (renderer trims / memory-block length)"
        ]
      },
      "v1_build_order": [
        "1. app/services/postprocess.py: summarize_call(messages) -> flat structured {summary,sentiment,resolution_status,primary_topic} via LiteLLM Gemini; fallback on failure",
        "2. Wire into end-of-call webhook (S4-D5 branch): feed Airtable write_interaction + SQLite calls row",
        "3. app/metrics.py: SQL aggregates over data/conversations.db -> report dict + CLI (python -m app.metrics)",
        "4. METRICS.md: ROI framework + prompt-tuning loop; fill numbers after live calls"
      ],
      "build_time_verifications_STRICT": [
        "LiteLLM + gemini-2.5-flash response_format json_schema returns valid {summary,sentiment,...} (flat schema, enable_json_schema_validation, fallback path)",
        "gemini-2.5-flash token pricing for est_cost_usd (web-verify current price, don't hardcode from memory)"
      ],
      "v1_scope": "Our synchronous post-call LLM pass -> {summary, sentiment, resolution_status, primary_topic} feeding Airtable + SQLite; PLUS app/metrics.py SQL compute; PLUS METRICS.md ROI skeleton.",
      "v2_scope": "Richer entity extraction, per-topic drill-downs, dashboards/visualization, automated prompt-tuning suggestions, cost alerting, CSAT correlation, date-range aggregation + export.",
      "open_questions": [],
      "next_steps": ["Build after S1 loop + S2 store exist (post-processing consumes state; metrics read the SQLite tables S2/S3 populate)"]
    }
  },
  "verified_facts": [
    "VAPI Custom LLM = our server exposes OpenAI-shaped POST /chat/completions; VAPI sends {model, messages, temperature, stream, tools?} + injected `call` object each turn.",
    "SSE response: `data: {chat.completion.chunk}\\n\\n`; [DONE] optional. Both streaming and non-streaming accepted.",
    "Caller phone at call.customer.number (nested, E.164). Tools passed in request; tool_calls detected in stream, backend executes + feeds result back."
  ],
  "build_order_proposed": [
    "S1 skeleton: /chat/completions echo loop wired to VAPI, prompt + minimal flow (verify VAPI can talk to our brain end-to-end)",
    "S2: tool-calling loop -> reuse crm lookup; write-back reused",
    "S1 depth: full state/flow/fallbacks + context management",
    "S4: post-processing (summary/sentiment/extraction) at call end",
    "S3: extend tracing across the loop (turns/latency/tokens/errors)",
    "S4: metrics compute + ROI writeup"
  ]
}
